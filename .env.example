# Copiado automaticamente pelo bootstrap.sh.
# Se quiser fazer manualmente:
#   ssh-keygen -t ed25519 -f ~/.ssh/devenv -N ""
#   cp .env.example .env
#   # cole o conteúdo de ~/.ssh/devenv.pub abaixo:
AUTHORIZED_KEY=ssh-ed25519 AAAA...sua-chave-publica-aqui... devenv@seu-mac
